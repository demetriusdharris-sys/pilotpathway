/**
 * Shared between the client component that enforces it in the browser, the
 * Server Action that enforces it again, and the CHECK constraint in 0032.
 * Three enforcement points, one number.
 */
export const MAX_REASON_CHARS = 2000;
